# Arona Frontend Configuration (optional)
#
# Notes:
# - Dev mode: `scripts/start_all.sh` uses Vite proxy by default, so you often don't need this file.
# - Production build / different origin deployment: set `VITE_BACKEND_URL` explicitly.
#
# If you still want to use a frontend-local env file:
#   cp env.example .env

# ============================================================================
# Backend API Configuration (Required)
# ============================================================================
# Backend API base URL
# Development: http://localhost:8000
# Production: https://your-api-domain.com
VITE_BACKEND_URL=http://localhost:8000

# ============================================================================
# Application Configuration
# ============================================================================
VITE_APP_NAME=Arona
VITE_APP_VERSION=0.1.0

# ============================================================================
# Development Configuration (Optional)
# ============================================================================
# Development server port (default: 5173)
# VITE_DEV_PORT=5173

# ============================================================================
# Notes
# ============================================================================
# - All environment variables must be prefixed with VITE_ to be accessible in the frontend
# - Changes to .env require restarting the development server
# - For production builds, set VITE_BACKEND_URL to your production API endpoint
